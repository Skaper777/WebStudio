'use strict';

(function () {
  var spans = document.querySelectorAll('.services__span');
  var svgs = document.querySelectorAll('.services__svg');
  
  for (var i = 0; i < spans.length; i++) {
    var span = spans[i];
    var svg = svgs[i];
  }
})();
